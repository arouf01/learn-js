// Date: 26 June 2025

// Date Types

// 1. String (text or characters)
let greeting = "Hello"; // Double quoted
let message = "Hi"; // Single quoted
let welcome = `Weolcome ${greeting} ${message}`; // template letarals

// 2. Numbers
let age = 20; // Integet Type
let pi = 3.14; // Float Type
let bigNumber = 1e6; // Big Number

// 3. Boolean Type (True or False)
let isSingle = false;
let isMarried = true;

// 4. Undefined (Declared but not Assigned)
let firstName;
console.log(firstName); // result: undefined

// 5. Null (No Value, Intentionally absense of any values or objets)
let emptyBox = null;

/*

Comparision and Use cases
=> String: text or messages
=> Numbers: calcutation, prices, counters
=> Boolean: Logical or conditions
=> Undefined: Uninitialized variables
=> Null: explicitly clear or reset or reassign variables

*/
